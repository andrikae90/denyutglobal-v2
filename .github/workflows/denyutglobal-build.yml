name: DenyutGlobal Build & Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply Patch 4 - Resend webhook signature verification
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('worker.ts')
          s = p.read_text()
          if 'RESEND_WEBHOOK_SIGNING_SECRET?: string;' in s:
              print('Patch 4 already present; nothing to do.')
              raise SystemExit(0)

          env_old = "  RESEND_API_KEY?: string;\n  NEWSLETTER_EMAIL_ENABLED?: string;"
          env_new = "  RESEND_API_KEY?: string;\n  RESEND_WEBHOOK_SIGNING_SECRET?: string;\n  NEWSLETTER_EMAIL_ENABLED?: string;"
          if env_old not in s:
              raise SystemExit('Env target not found')
          s = s.replace(env_old, env_new, 1)

          marker = "// 5.8 RESEND WEBHOOK LISTENER (POST /api/webhooks/resend)"
          if marker not in s:
              raise SystemExit('Webhook marker not found')

          helper = '''async function verifyResendWebhookSignature(request: Request, rawBody: string, env: Env): Promise<boolean> {
  const secret = (env.RESEND_WEBHOOK_SIGNING_SECRET || '').trim();
  if (!secret) return false;
  const svixId = request.headers.get('svix-id');
  const svixTimestamp = request.headers.get('svix-timestamp');
  const svixSignature = request.headers.get('svix-signature');
  if (!svixId || !svixTimestamp || !svixSignature) return false;
  const timestamp = Number(svixTimestamp);
  if (!Number.isFinite(timestamp)) return false;
  const ageSeconds = Math.abs(Math.floor(Date.now() / 1000) - timestamp);
  if (ageSeconds > 300) return false;

  const secretValue = secret.startsWith('whsec_') ? secret.slice(6) : secret;
  let secretBytes: Uint8Array;
  try {
    const normalized = secretValue.replace(/-/g, '+').replace(/_/g, '/');
    const padded = normalized + '='.repeat((4 - (normalized.length % 4)) % 4);
    const binary = atob(padded);
    secretBytes = Uint8Array.from(binary, (char) => char.charCodeAt(0));
  } catch {
    return false;
  }

  try {
    const key = await crypto.subtle.importKey('raw', secretBytes, { name: 'HMAC', hash: 'SHA-256' }, false, ['verify']);
    const signedPayload = `${svixId}.${svixTimestamp}.${rawBody}`;
    for (const entry of svixSignature.split(' ').map((v) => v.trim()).filter(Boolean)) {
      const match = /^v1,(.+)$/.exec(entry);
      if (!match) continue;
      try {
        const normalizedSignature = match[1].replace(/-/g, '+').replace(/_/g, '/');
        const paddedSignature = normalizedSignature + '='.repeat((4 - (normalizedSignature.length % 4)) % 4);
        const binarySignature = atob(paddedSignature);
        const signatureBytes = Uint8Array.from(binarySignature, (char) => char.charCodeAt(0));
        if (await crypto.subtle.verify('HMAC', key, signatureBytes, new TextEncoder().encode(signedPayload))) return true;
      } catch {}
    }
  } catch (err) {
    console.error('Resend webhook signature verification error:', err);
  }
  return false;
}

'''
          s = s.replace(marker, helper + marker, 1)

          old = '''    // 5.8 RESEND WEBHOOK LISTENER (POST /api/webhooks/resend)
    if (pathname === '/api/webhooks/resend' && method === 'POST') {
      try {
        const body: any = await request.json().catch(() => ({}));
        if (body?.type && body?.data?.email && env.DB) {
          const targetEmail = body.data.email.toLowerCase();
          if (body.type === 'email.bounced' || body.type === 'email.complained') {
            await executeWorkerD1Query(
              env.DB,
              `UPDATE subscribers SET status = 'unsubscribed', unsubscribed_at = datetime('now') WHERE email = ?;`,
              [targetEmail]
            );
          }
        }
        return jsonResponse({ received: true });
      } catch (e) {
        return jsonResponse({ received: true });
      }
    }
'''
          new = '''    // 5.8 RESEND WEBHOOK LISTENER (POST /api/webhooks/resend)
    if (pathname === '/api/webhooks/resend') {
      if (method !== 'POST') {
        return jsonResponse({ received: false, error: 'Method not allowed.' }, 405, { 'Allow': 'POST' });
      }
      try {
        const rawBody = await request.text();
        if (!await verifyResendWebhookSignature(request, rawBody, env)) {
          return jsonResponse({ received: false, error: 'Invalid webhook signature.' }, 401, { 'Cache-Control': 'no-store', 'X-Robots-Tag': 'noindex, nofollow' });
        }
        let body: any;
        try {
          body = JSON.parse(rawBody);
        } catch {
          return jsonResponse({ received: false, error: 'Invalid JSON payload.' }, 400, { 'Cache-Control': 'no-store', 'X-Robots-Tag': 'noindex, nofollow' });
        }
        if (!env.DB) {
          return jsonResponse({ received: false, error: 'Cloudflare D1 is unavailable.' }, 503, { 'Cache-Control': 'no-store', 'X-Robots-Tag': 'noindex, nofollow' });
        }
        if (body?.type && body?.data?.email) {
          const targetEmail = typeof body.data.email === 'string' ? body.data.email.trim().toLowerCase() : '';
          if (targetEmail && (body.type === 'email.bounced' || body.type === 'email.complained')) {
            const updateRes = await executeWorkerD1Query(env.DB, `UPDATE subscribers SET status = 'unsubscribed', unsubscribed_at = datetime('now') WHERE email = ?;`, [targetEmail]);
            if (!updateRes.success) {
              return jsonResponse({ received: false, error: 'Failed to update subscriber status.' }, 503);
            }
          }
        }
        return jsonResponse({ received: true });
      } catch (e) {
        console.error('Resend webhook error:', e);
        return jsonResponse({ received: false, error: 'Webhook processing failed.' }, 500);
      }
    }
'''
          if old not in s:
              raise SystemExit('Original webhook block not found')
          s = s.replace(old, new, 1)
          p.write_text(s)
          PY

      - name: Install dependencies
        run: npm install

      - name: Type check
        run: npm run lint

      - name: Build application
        run: npm run build

      - name: Deploy to Cloudflare Worker
        run: npx wrangler deploy --config wrangler.jsonc
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Commit Patch 4 and remove temporary workflow
        if: contains(github.event.head_commit.message, 'security: run Resend webhook hardening in trusted build workflow')
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add worker.ts
          git rm .github/workflows/security-step-4-patch.yml
          git checkout HEAD^ -- .github/workflows/denyutglobal-build.yml
          git add .github/workflows/denyutglobal-build.yml
          git commit -m 'security: verify Resend webhook signatures before processing'
          git push
